# Template for .env. Copy to .env and fill in the password.
# .env is git-ignored (never committed) but IS uploaded to Cloud Run, so the
# deployed backend reads these at runtime. store.py builds a mongodb+srv:// URI.
MONGODB_URL=mongodb+srv://cluster0.1a9uiru.mongodb.net/
MONGODB_USERNAME=evertune_loadtest
MONGODB_PASSWORD=
